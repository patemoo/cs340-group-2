<h1>Products</h1>

<ul class="products-list">
    {{#each products}}
    <li>
        <h2>{{this.name}}</h2>
        <p>{{this.price}}</p>
        <p>sku: {{this.sku}}</p>
        <a href="/products/{{this.id}}">view</a>
    </li>
    {{/each}}
</ul>